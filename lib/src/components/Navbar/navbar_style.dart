import 'package:flutter/material.dart';

class NavBarMiniStyle {
  static const navElements = TextStyle(
      fontSize: 13,
      fontFamily: 'Chivo',
      letterSpacing: -0.15,
      color: Color.fromRGBO(162, 166, 187, 1));

  static const navElementsHover = TextStyle(
      fontSize: 12,
      fontFamily: 'Chivo',
      letterSpacing: -0.15,
      color: Color.fromRGBO(79, 90, 191, 1));
}
